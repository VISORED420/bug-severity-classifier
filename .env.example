# Copy this file to `.env` and fill in your values (never commit `.env`).
# Nothing here is required for the current code — this is a placeholder
# for future integrations (e.g. Bugzilla REST API key).

# BUGZILLA_API_KEY=
# BUGZILLA_BASE_URL=https://bugs.eclipse.org/bugs/rest
